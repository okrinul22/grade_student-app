// API Configuration
// In development: use /api (proxied by Vite to localhost:3000)
// In production: use /api (proxied by nginx to backend container)

const API_BASE_URL = '/api'

export default API_BASE_URL
